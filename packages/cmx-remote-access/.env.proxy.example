# Copy to .env when running the integrated dev proxy (optional).
# Base URL of the station API (e.g. active-cell-api).
CMX_PROXY_UPSTREAM_URL=http://127.0.0.1:8765

# CMX_PROXY_HOST=127.0.0.1
# CMX_PROXY_PORT=8780
